apiVersion: apps/v1
kind: Deployment
metadata:
  name: github-personal-stats
spec:
  replicas: 1
  selector:
    matchLabels:
      app: github-personal-stats
  template:
    metadata:
      labels:
        app: github-personal-stats
    spec:
      containers:
        - name: github-personal-stats
          image: ghcr.io/liuchong/github-personal-stats:latest
          ports:
            - containerPort: 3000
          env:
            - name: GITHUB_PERSONAL_STATS_ADDR
              value: 0.0.0.0:3000
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: github-personal-stats
spec:
  selector:
    app: github-personal-stats
  ports:
    - port: 80
      targetPort: 3000
